# Archived plotting notebooks

Frozen. These notebooks predate the scripted pipeline and use stale
tiered-density breakpoints, so their figures should not be trusted.

They are superseded by:

- `analysis_scripts/plotlib/` — the shared seaborn plotting layer
- `analysis_scripts/paper_wake/` — figures for the wake manuscript
- `analysis_scripts/paper_sensitivity/` — figures for the sensitivity manuscript

Kept only as a record of the original exploratory analysis. The notebooks are
gitignored (`*.ipynb`), so they exist here on disk and not in git history.
